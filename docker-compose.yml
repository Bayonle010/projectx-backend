version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: project-x-app
    ports:
      - "8082:8082"
    environment:
      SPRING_PROFILES_ACTIVE: docker
    restart: unless-stopped





networks:
  project-x-network:
    driver: bridge